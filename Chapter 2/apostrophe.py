#this is to test and show syntax errors

message = "One of Python's strengths is its diverse community"
print(message) #this works because single quote is within double

message = 'One of Python's strengths is its diverse community'
print(message) #this throws syntax error because compiler reads the apostrophe as the end quote
